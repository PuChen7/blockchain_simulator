const Blockchain = require('./Blockchain');
const Block = require('./block');

describe('Blockchain', () => {
    let bc;

    beforeEach(() => {
        bc = new Blockchain();  // new instance
    });

});